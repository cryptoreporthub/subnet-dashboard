# Release 2026-09-01 — production deploy request

Base for this deploy: main at f0d221a2 (deploy workflow checks out this PR's head SHA).

## Ships (6 commits ahead of production fe33ca5b)

- 9202496b — docs: Agent Operating Rules (2026-09-01)
- 6b29d3eb — docs: typo fix
- 44fd2fb7 — fix(#1112): restore _last_resolver_tick compat shim + loud stall-guard probe failures
- 547c52d5 — fix(resolver): bound direct liveness snapshot fallback (#1152)
- bc79cf7e — chore(diagnostics): bounded Fly incident capture workflow (#1153)
- f0d221a2 — chore(diagnostics): label-trigger for incident capture (#1154)

## Rationale

Production resolver is failing (persisted_state_stale, running=false, lifecycle stopped) per
/api/predictions/resolver and /api/learning/health (degraded, resolver stale ~2.7h).
The two resolver fix commits above target this exact failure mode.

## Pre-deploy evidence

- Read-only incident capture artifact fly-incident-read-only-33551194993 (created 19:44Z, 7-day retention) — captured BEFORE any restart.
- Public receipts regenerated 20:08-20:10Z: /health 200 OK; readiness DEGRADED (resolver failing, watchdog pending=5, oldest 19.9h); live feed fresh (149 subnets); worker heartbeat fresh.

## Safety

- Deploy via the guarded fly-deploy label path only (actor cryptoreporthub, same-repo PR).
- Deploy Guard re-runs endpoint contract tests before the deploy step.
- Post-deploy steps verify topology (web=1, worker=0) and v1 inline readiness.
