"""Server configuration — imports, constants, and safe fallbacks."""
import jsonimport loggingimport mathimport osimport sysimport threadingimport timeimport tracebackimport urllib.requestfrom contextlib import asynccontextmanagerfrom datetime import datetimefrom typing import Any, AsyncIterator, Dict, List, Optionalfrom fastapi import FastAPI, Requestfrom fastapi.middleware.cors import CORSMiddlewarefrom fastapi.responses import PlainTextResponsefrom fastapi.staticfiles import StaticFilesfrom fastapi.templating import Jinja2Templates# Add the current directory to path for importssys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))from fetchers.taomarketcap import get_all_subnets, get_subnet_data# ---------------------------------------------------------------------------# Safe internal imports — endpoints stay live even if modules are incomplete.# ---------------------------------------------------------------------------try:    from internal.council.state_vector import (        score_subnet_for_hour,        score_subnet_for_day,        clamp_prediction_horizon,    )except Exception:  # pragma: no cover    def score_subnet_for_hour(*_args, **_kwargs):        return 0.0    def score_subnet_for_day(*_args, **_kwargs):        return 0.0    def clamp_prediction_horizon(horizon: int, predicted_pct: Optional[float] = None) -> int:        """Inline fallback: HARD 4-hour maximum for every prediction horizon."""        return max(1, min(int(horizon), 4))# Canonical percentage-based prediction horizon clamp. Delegates to the shared# implementation in ``internal.council.state_vector`` (with the safe fallback# above when that module is unavailable). Apply this at every point where a# prediction's horizon is determined, BEFORE the prediction is stored/displayed.## NOTE: existing predictions already persisted with the old generic 1-168h clamp# (e.g. a -2.1% move pinned to 168h) will keep their stale horizon until they# either expire naturally or are re-clamped by ``_reclamp_stored_predictions``# on startup (see ``_lifespan``)._clamp_prediction_horizon = clamp_prediction_horizontry:    from internal.council.daily_pick import select_daily_pickexcept Exception:  # pragma: no cover    def select_daily_pick(*_args, **_kwargs):        return Nonetry:    from internal.council.daily_pick_engine import get_or_create_today_pickexcept Exception:  # pragma: no cover    def get_or_create_today_pick(*_args, **_kwargs):        return {}try:    from internal.council.hourly_pick import select_hourly_pickexcept Exception:  # pragma: no cover    def select_hourly_pick(*_args, **_kwargs):        return {"subnet": None, "score": 0.0, "confidence": 0.0, "expert_contributions": {}, "scenario_tags": {}, "audit": {"approved": False, "concerns": ["hourly_pick unavailable"], "adjusted_confidence": 0.0}, "final_confidence": 0.0, "action": "long"}try:    from internal.council import pick_historyexcept Exception:  # pragma: no cover    pick_history = None  # type: ignore[assignment]try:    from internal import freshness_trackerexcept Exception:  # pragma: no cover    freshness_tracker = None  # type: ignore[assignment]try:    from internal.council import resolver, scenario_memory, rotation_trackerexcept Exception:  # pragma: no cover    class _FakeResolver:        @staticmethod        def resolve_due_predictions(*_args, **_kwargs):            return {"resolved_now": [], "resolved": [], "pending": [], "stats": {}}        @staticmethod        def get_resolved_predictions(*_args, **_kwargs):            return {"resolved": [], "pending": [], "stats": {}}    resolver = _FakeResolver()    scenario_memory = type(        "_FakeModule",        (),        {            "get_scenarios": lambda *_args, **_kwargs: [],            "add_scenario": lambda *_args, **_kwargs: {},            "classify_regime": lambda *_args, **_kwargs: "neutral",            "get_memory_snapshot": lambda *_args, **_kwargs: {                "scenarios": [],                "regimes": {},                "stats": {"total": 0, "by_regime": {}},                "meta": {},            },        },    )()    rotation_tracker = type("_FakeModule", (), {"get_rotation_summary": lambda *_args, **_kwargs: {}})()try:    from internal.judges import all_judges, get_judge, on_prediction_created, on_prediction_resolved    from internal.judges.portfolios import all_portfolios    from internal.judges.postmortems import all_postmortems, list_for_judge    _JUDGES_AVAILABLE = Trueexcept Exception as _judges_import_exc:  # pragma: no cover    _JUDGES_AVAILABLE = False    logger = logging.getLogger(__name__)    logger.warning("Judge layer unavailable: %s", _judges_import_exc)    def all_judges():        return []    def get_judge(name):        return None    def on_prediction_created(*_args, **_kwargs):        return {}    def on_prediction_resolved(*_args, **_kwargs):        return {}    def all_portfolios():        return {}    def all_postmortems():        return {}    def list_for_judge(name):        return []# Per-subnet judge scoring (new council surface). Safe fallback keeps the app# booting even if the module is incomplete or its dependencies are missing.try:    from internal.judges.subnet_judges import score_all_subnets, score_subnet    _SUBNET_JUDGES_AVAILABLE = Trueexcept Exception as _subnet_judges_import_exc:  # pragma: no cover    _SUBNET_JUDGES_AVAILABLE = False    logger = logging.getLogger(__name__)    logger.warning("Subnet judge layer unavailable: %s", _subnet_judges_import_exc)    def score_all_subnets(*_args, **_kwargs):        return []    def score_subnet(*_args, **_kwargs):        return {}try:    from internal.council.weights import load_weights, save_weightsexcept Exception:  # pragma: no cover    _DEFAULT_WEIGHTS = {"quant": 1.0, "hype": 1.0, "contrarian": 1.0, "technical": 1.0}    def load_weights(*_args, **_kwargs):        return dict(_DEFAULT_WEIGHTS)    def save_weights(*_args, **_kwargs):        passtry:    from internal.indicators import (        IndicatorEngine,        get_indicator_scheduler_state,        start_indicator_scheduler,        stop_indicator_scheduler,    )except Exception:  # pragma: no cover    class IndicatorEngine:        pass    def get_indicator_scheduler_state(*_args, **_kwargs):        return {"running": False}    def start_indicator_scheduler(*_args, **_kwargs):        pass    def stop_indicator_scheduler(*_args, **_kwargs):        pass# Prediction resolver scheduler — grades pending predictions on a clock so the# learning loop's weights/accuracy update even when no dashboard is rendered.try:    from internal.council.resolver_scheduler import (        get_prediction_resolver_scheduler,        get_prediction_resolver_scheduler_state,        start_prediction_resolver_scheduler,        stop_prediction_resolver_scheduler,    )except Exception:  # pragma: no cover    def get_prediction_resolver_scheduler_state(*_args, **_kwargs):        return {"running": False}    def start_prediction_resolver_scheduler(*_args, **_kwargs):        pass    def stop_prediction_resolver_scheduler(*_args, **_kwargs):        pass    def get_prediction_resolver_scheduler(*_args, **_kwargs):        return Nonetry:    from datastore.learning_engine import LearningEngineexcept Exception:  # pragma: no cover    class LearningEngine:        def __init__(self, *args, **kwargs):            passtry:    from internal.pump_tracker import (        get_pump_tracker,        get_all_profiles,        get_current_phases,        get_recent_cycles,        get_cycle_analytics_accuracy,        get_pump_tracker_state,    )    _PUMP_TRACKER_AVAILABLE = Trueexcept Exception:  # pragma: no cover    _PUMP_TRACKER_AVAILABLE = False    def get_pump_tracker(*_args, **_kwargs):        return None    def get_all_profiles(*_args, **_kwargs):  # type: ignore[no-redef]        return {}    def get_current_phases(*_args, **_kwargs):  # type: ignore[no-redef]        return {}    def get_recent_cycles(*_args, **_kwargs):  # type: ignore[no-redef]        return []    def get_cycle_analytics_accuracy(*_args, **_kwargs):  # type: ignore[no-redef]        return {"status": "success", "accuracy": {}}    def get_pump_tracker_state(*_args, **_kwargs):  # type: ignore[no-redef]        return {"status": "error", "error": "pump tracker unavailable"}try:    from message_intel import Database as _MessageIntelDatabase    from message_intel import NLPAnalyzer as _MessageIntelNLPAnalyzer    from message_intel import JuryBridge as _MessageIntelJuryBridge    from message_intel import PriceTracker as _MessageIntelPriceTracker    from message_intel import SelfLearning as _MessageIntelSelfLearning    _MESSAGE_INTEL_AVAILABLE = Trueexcept Exception as _message_intel_import_exc:    _MESSAGE_INTEL_AVAILABLE = False    logger = logging.getLogger(__name__)    logger.warning("Message intelligence package unavailable: %s", _message_intel_import_exc)logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(name)s %(message)s")logger = logging.getLogger(__name__)# Runtime data directory. In production this is a Fly.io persistent volume# mount point (see fly.toml [mounts]); locally it is just ./data. The directory# is created on startup so the app works with or without a mounted volume.DATA_DIR = os.environ.get("DATA_DIR", "data")DATA_SEEDS_DIR = os.environ.get("DATA_SEEDS_DIR", "data_seeds")# Learning-critical files that should survive deploys. On the first boot of an# empty volume these are seeded from data_seeds/ so prior learning is preserved;# they are never overwritten once present on the volume._PERSISTENT_DATA_FILES = ("predictions.json", "soul_map.json")def _ensure_data_dir() -> None:    """Create the data directory if missing and diagnose persistence state.    - Ensures ``DATA_DIR`` exists (works with or without a Fly volume).    - Seeds learning-critical files from ``DATA_SEEDS_DIR`` when the volume is      empty on first boot (never overwrites existing data).    - Logs a warning for each missing/empty persistent file so future resets      are diagnosable from the app logs.    """    try:        os.makedirs(DATA_DIR, exist_ok=True)    except Exception as exc:        logger.warning("Could not create data directory %s: %s", DATA_DIR, exc)        return    # Seed missing learning-critical files from packaged defaults so an empty    # volume does not silently discard prior learning on first boot.    for name in _PERSISTENT_DATA_FILES:        target = os.path.join(DATA_DIR, name)        if os.path.exists(target) and os.path.getsize(target) > 0:            continue        seed = os.path.join(DATA_SEEDS_DIR, name)        if os.path.exists(seed) and os.path.getsize(seed) > 0:            try:                import shutil                shutil.copy2(seed, target)                logger.info(                    "Seeded empty/missing data file %s from %s (first boot of "                    "persistent volume).", name, DATA_SEEDS_DIR                )            except Exception as exc:                logger.warning("Could not seed %s from %s: %s", name, seed, exc)    # Diagnostic logging: surface missing/empty persistent files so future    # learning-data resets are visible in the app logs.    for name in _PERSISTENT_DATA_FILES:        path = os.path.join(DATA_DIR, name)        try:            if not os.path.exists(path):                logger.warning(                    "Learning data file missing on startup: %s "                    "(data may have been reset or the volume is empty).", path                )            elif os.path.getsize(path) == 0:                logger.warning(                    "Learning data file is empty on startup: %s "                    "(data may have been reset or the volume is empty).", path                )            else:                logger.info("Learning data file present on startup: %s (%d bytes)", path, os.path.getsize(path))        except Exception as exc:            logger.warning("Could not stat %s: %s", path, exc)_ensure_data_dir()def _mark_fresh(key: str) -> None:    """Mark a dashboard section as freshly updated (no-op if tracker missing)."""    if freshness_tracker is not None:        try:            freshness_tracker.mark_updated(key)        except Exception:            passdef _freshness_snapshot() -> Dict[str, Any]:    """Return the freshness map for /api/freshness (safe default on failure)."""    if freshness_tracker is not None:        try:            return freshness_tracker.snapshot()  # type: ignore[no-any-return]        except Exception:            pass    return {"last_updated": {}, "now": datetime.utcnow().isoformat() + "Z"}def _record_hour_pick(pick: Dict[str, Any], subnets: List[Dict[str, Any]],                      indicators: Optional[Dict[str, Any]] = None) -> Dict[str, Any]:    """Track a Pick of the Hour selection (entry price/trigger/outcome).    Delegates to ``internal.council.pick_history`` so the #1 hourly pick    carries ``selected_at``/``entry_price``/``trigger_reason`` and a live    vs-market success metric. Returns the pick unchanged if the module is    unavailable so the render path never breaks.    """    if pick_history is None:        return pick    try:        return pick_history.record_pick(pick, subnets, indicators)    except Exception as exc:        logger.warning("pick_history.record_pick failed: %s", exc)        return pickdef _hour_pick_history(limit: int = 20) -> Dict[str, Any]:    """Return the pick-of-the-hour history + aggregate success stats."""    if pick_history is None:        return {"active": None, "history": [], "stats": {"total": 0, "wins": 0, "losses": 0, "success_rate": 0.0}}    try:        return pick_history.get_history(limit=limit)  # type: ignore[no-any-return]    except Exception as exc:        logger.warning("pick_history.get_history failed: %s", exc)        return {"active": None, "history": [], "stats": {"total": 0, "wins": 0, "losses": 0, "success_rate": 0.0}}# ---------------------------------------------------------------------------# Message Intelligence pipeline singletons (Telegram → NLP → Jury → Learning)# ---------------------------------------------------------------------------_message_intel_db = None_message_intel_nlp = None_message_intel_jury = None_message_intel_price_tracker = None_message_intel_self_learning = Nonedef _get_message_intel_db():    global _message_intel_db    if _message_intel_db is None and _MESSAGE_INTEL_AVAILABLE:        _message_intel_db = _MessageIntelDatabase()    return _message_intel_dbdef _get_message_intel_nlp():    global _message_intel_nlp    if _message_intel_nlp is None and _MESSAGE_INTEL_AVAILABLE:        _message_intel_nlp = _MessageIntelNLPAnalyzer()    return _message_intel_nlpdef _get_message_intel_jury():    global _message_intel_jury    if _message_intel_jury is None and _MESSAGE_INTEL_AVAILABLE:        _message_intel_jury = _MessageIntelJuryBridge()    return _message_intel_jurydef _get_message_intel_price_tracker():    global _message_intel_price_tracker    if _message_intel_price_tracker is None and _MESSAGE_INTEL_AVAILABLE:        _message_intel_price_tracker = _MessageIntelPriceTracker()        _message_intel_price_tracker.set_db(_get_message_intel_db())    return _message_intel_price_trackerdef _get_message_intel_self_learning():    global _message_intel_self_learning    if _message_intel_self_learning is None and _MESSAGE_INTEL_AVAILABLE:        _message_intel_self_learning = _MessageIntelSelfLearning()        _message_intel_self_learning.set_db(_get_message_intel_db())    return _message_intel_self_learningdef _start_telegram_listener() -> None:    """Auto-start the Telegram listener if credentials are configured.    Uses lazy imports and a try/except so the app still starts if telethon    is missing or authentication fails. Runs in a daemon thread so startup    is not blocked.    """    api_id = os.environ.get("TELEGRAM_API_ID", "").strip()    api_hash = os.environ.get("TELEGRAM_API_HASH", "").strip()    phone = os.environ.get("TELEGRAM_PHONE", "").strip()    if not api_id or not api_hash or not phone:        logger.info(            "Telegram listener skipped: TELEGRAM_API_ID, TELEGRAM_API_HASH, and "            "TELEGRAM_PHONE must all be set."        )        return    group = os.environ.get("TELEGRAM_GROUP", "OfficialSubnetSummer").strip() or "OfficialSubnetSummer"    try:        from message_intel.telegram_listener import TelegramListener  # lazy import        listener = TelegramListener(            api_id=api_id,            api_hash=api_hash,            phone=phone,            group=group,            session_name=os.environ.get("TELEGRAM_SESSION_NAME", "telegram_listener"),        )        # Run start() in a daemon thread so it does not block app startup.        def _run():            try:                listener.start()                logger.info("Telegram listener auto-started in background thread.")            except Exception as exc:                logger.warning("Telegram listener failed to start: %s", exc)        thread = threading.Thread(target=_run, daemon=True, name="telegram-listener-starter")        thread.start()    except Exception as exc:        logger.warning("Telegram listener auto-start error: %s", exc)@asynccontextmanagerasync def _lifespan(app: FastAPI) -> AsyncIterator[None]:    """Start background services on startup and stop them on shutdown."""    # Ensure the persistent data directory exists and diagnose any missing    # learning-data files (e.g. an empty/unmounted Fly volume) on every boot.    try:        _ensure_data_dir()    except Exception as exc:        logger.warning("Startup data-dir check skipped: %s", exc)    # Re-clamp any stale pending predictions to the magnitude-based horizon    # bands so legacy entries (e.g. a -2.1% move pinned to 168h) expire on a    # sensible schedule instead of lingering for up to a week.    try:        _reclamp_stored_predictions()    except Exception as exc:        logger.warning("Startup prediction re-clamp skipped: %s", exc)    try:        start_indicator_scheduler()        logger.info("Indicator scheduler started")    except Exception as exc:        logger.warning("Failed to start indicator scheduler: %s", exc)    # Start the prediction resolver on a clock so pending predictions get    # graded (and learning-loop weights updated) even when no dashboard is    # being rendered. The first tick runs shortly after boot to clear any    # backlog of stuck ``pending`` predictions.    try:        start_prediction_resolver_scheduler(immediate=False)        logger.info("Prediction resolver scheduler started")    except Exception as exc:        logger.warning("Failed to start prediction resolver scheduler: %s", exc)    _start_telegram_listener()    # Start the per-subnet judge council score refresh scheduler. It runs in a    # background thread so startup is not blocked and degrades gracefully if the    # subnet data source is temporarily unavailable.    try:        if _SUBNET_JUDGES_AVAILABLE:            judge_scheduler_thread = threading.Thread(target=_refresh_judge_scores, daemon=True)            judge_scheduler_thread.start()            logger.info("Judge score scheduler started")    except Exception as exc:        logger.warning("Failed to start judge score scheduler: %s", exc)    # Start per-subnet baseline price recording (env-gated, default on).    try:        enabled = os.environ.get("ENABLE_BASELINE_RECORDING", "true").lower()        if enabled in ("1", "true", "yes", "on"):            interval = int(os.environ.get("BASELINE_INTERVAL_SECONDS", "300"))            tracker = _get_message_intel_price_tracker()            if tracker is not None:                tracker.start_baseline_recording(interval=interval)                logger.info("Baseline price recording started (interval=%ds)", interval)    except Exception as exc:        logger.warning("Failed to start baseline price recording: %s", exc)    yield    try:        stop_prediction_resolver_scheduler()        logger.info("Prediction resolver scheduler stopped")    except Exception as exc:        logger.warning("Failed to stop prediction resolver scheduler: %s", exc)    try:        stop_indicator_scheduler()        logger.info("Indicator scheduler stopped")    except Exception as exc:        logger.warning("Failed to stop indicator scheduler: %s", exc)app = FastAPI(    title="SimiVision Subnet Dashboard",    version="3.5.0",    lifespan=_lifespan,)# CORS middleware (replaces Flask's per-response CORS headers)app.add_middleware(    CORSMiddleware,    allow_origins=["*"],    allow_credentials=True,    allow_methods=["*"],    allow_headers=["*"],)# Mount static files at /static_static_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "static")if os.path.isdir(_static_dir):    app.mount("/static", StaticFiles(directory=_static_dir), name="static")# Jinja2 templates for server-side rendered dashboard_templates_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), "templates")templates = Jinja2Templates(directory=_templates_dir)# Disable the Jinja2 template cache to avoid unhashable-type errors# with Starlette 1.0.0+ (the cache_key can contain unhashable dicts).templates.env.cache_size = 0templates.env.cache = Nonedef _jinja_safe_list(value: Any) -> List[Any]:    """Return a safe list for Jinja iteration.    Strings and bytes are returned as single-item lists so they are not    iterated character-by-character. Dicts with unhashable keys are converted    to a list of their values to avoid Jinja's "unhashable type" errors.    """    if value is None:        return []    if isinstance(value, (str, bytes)):        return [value]    if isinstance(value, dict):        # If any key is unhashable, iterating the dict raises. Fall back to        # values() so the page still renders.        try:            list(value.keys())        except TypeError:            return list(value.values())        return list(value.values())    try:        return list(value)    except Exception:        return [value] if value else []templates.env.filters["safe_list"] = _jinja_safe_listdef _app_version() -> str:    return "3.5.0"_APP_VERSION = _app_version()_ROTATION_TOKENS = ["hyperliquid", "vvv", "near", "render", "fetch"]# CoinGecko coin-id mapping for the rotation watchlist. Symbols that CoinGecko# does not list under their ticker are mapped to their canonical coin id._ROTATION_COINGECKO_IDS = {    "hyperliquid": "hyperliquid",    "vvv": "venice-token",    "near": "near",    "render": "render-token",    "fetch": "fetch-ai",}# In-process cache for rotation-token prices (shared by /api/rotation-tokens)._ROTATION_PRICE_CACHE: Dict[str, Any] = {"data": None, "at": 0.0}_ROTATION_PRICE_CACHE_TTL = 60  # secondsdef _fetch_rotation_token_prices() -> Dict[str, Dict[str, Any]]:    """Fetch current USD prices + 24h change for the rotation watchlist.    Uses CoinGecko's public simple/price endpoint (no API key required) and    caches the result for 60 seconds. Returns a dict keyed by lower-case    symbol, e.g. ``{"near": {"price": 1.79, "price_change_24h": 0.67}}``.    On failure returns the last cached value (or an empty dict).    """    now = time.time()    cached = _ROTATION_PRICE_CACHE.get("data")    if cached is not None and (now - _ROTATION_PRICE_CACHE.get("at", 0.0)) < _ROTATION_PRICE_CACHE_TTL:        return cached    ids = ",".join(        _ROTATION_COINGECKO_IDS[sym]        for sym in _ROTATION_TOKENS        if sym in _ROTATION_COINGECKO_IDS    )    result: Dict[str, Dict[str, Any]] = {}    if ids:        try:            url = (                "https://api.coingecko.com/api/v3/simple/price"                f"?ids={ids}&vs_currencies=usd&include_24hr_change=true"            )            req = urllib.request.Request(url, headers={"User-Agent": "SubnetDashboard/1.0"})            with urllib.request.urlopen(req, timeout=10) as resp:                payload = json.loads(resp.read().decode())        except Exception as exc:            logger.warning("CoinGecko rotation-token price fetch failed: %s", exc)            payload = None        if isinstance(payload, dict):            for sym in _ROTATION_TOKENS:                coin_id = _ROTATION_COINGECKO_IDS.get(sym)                entry = payload.get(coin_id) if coin_id else None                if isinstance(entry, dict):                    price = entry.get("usd")                    change = entry.get("usd_24h_change")                    result[sym] = {                        "price": float(price) if price is not None else None,                        "price_change_24h": round(float(change), 2) if change is not None else None,                    }    if result:        _ROTATION_PRICE_CACHE["data"] = result        _ROTATION_PRICE_CACHE["at"] = now        return result    # Fall back to stale cache so transient CoinGecko failures don't null out    # previously-known prices.    return cached or {}# ---------------------------------------------------------------------------# SimiVision chat helpers (Phase 4: LLM interaction with mindmap context)# ---------------------------------------------------------------------------def _build_simivision_prompt(message: str, context: Dict[str, Any]) -> str:    """Build a prompt that fuses the user message with live SimiVision + soul_map context."""    top = context.get("simivision_picks", [])    picks_str = "; ".join(        f"#{p.get('rank')} {p.get('name')} (SN{p.get('netuid')}) "        f"emission={p.get('emission')} apy={p.get('apy')} "        f"chg24h={p.get('price_change_24h')}% conviction={p.get('conviction')} "        f"rec={p.get('recommendation')}"        for p in top    ) or "No picks available"    weights = context.get("expert_weights", {})    weights_str = ", ".join(f"{k}={v}" for k, v in weights.items()) or "none"    return (        "You are SimiVision, an AI analyst for Bittensor subnets. "        "Use the live subnet snapshot and the Council's learned expert weights below.\n\n"        f"User question: {message}\n\n"        f"Top SimiVision picks: {picks_str}\n"        f"Source: {context.get('source', 'unknown')}\n"        f"Council expert weights (self-learning loop): {weights_str}\n"        "Answer concisely and tie the reasoning back to the picks and expert weights."    )def _call_llm(prompt: str, message: str, context: Dict[str, Any]) -> tuple[str, bool]:    """Call an LLM API when configured, otherwise fall back to the local explainer.    Returns (reply, llm_used). The local fallback keeps the endpoint fully    functional in environments without an LLM API key while still integrating    the mindmap / self-learning context.    """    # Prefer Chutes AI env vars    api_key = (        os.environ.get("CHUTES_API_KEY")        or os.environ.get("OPENAI_API_KEY")        or os.environ.get("LLM_API_KEY")    )    base_url = os.environ.get("CHUTES_BASE_URL") or os.environ.get("LLM_BASE_URL", "https://api.chutes.ai/v1")    model = os.environ.get("CHUTES_MODEL") or os.environ.get("LLM_MODEL", "deepseek-ai/DeepSeek-V3.2-TEE")    if api_key:        try:            import requests            resp = requests.post(                f"{base_url.rstrip('/')}/chat/completions",                headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},                json={                    "model": model,                    "messages": [                        {"role": "system", "content": "You are SimiVision, a Bittensor subnet analyst."},                        {"role": "user", "content": prompt},                    ],                    "max_tokens": 400,                },                timeout=20,            )            if resp.status_code == 200:                data = resp.json()                reply = data.get("choices", [{}])[0].get("message", {}).get("content", "")                if reply:                    return reply.strip(), True            logger.warning("LLM API call failed (%s); falling back to local explainer", resp.status_code)        except Exception as exc:            logger.warning("LLM API call errored (%s); falling back to local explainer", exc)    # Local fallback: reuse the existing SimiVision explainer so the loop stays intact.    try:        from internal.llm.explainer import generate_ai_response        return generate_ai_response(message, context), False    except Exception as exc:        logger.warning("Local explainer failed (%s); returning canned reply", exc)        return (            "SimiVision is online. I can explain top subnet picks, compare APY, "            "or analyze market trends. What would you like to know?",            False,        )# ---------------------------------------------------------------------------# Fast, fail-safe endpoints# These are registered first so they win even if older route definitions below# still exist in the file or in a stale deployment.# ---------------------------------------------------------------------------def _get_subnets_with_source() -> tuple[List[Dict[str, Any]], str]:    """Return subnets with source tracking.        Uses taomarketcap API with caching (5 min TTL).    Returns (subnets, source) where source is one of:    - "taomarketcap" (live data)    - "taomarketcap-cache" (stale cache)    - "static-fallback" (no cache available)    """    cache_path = os.path.join("data", "subnets.db")    db_exists = os.path.exists(cache_path)        try:        subnets = get_all_subnets()        if subnets:            # Determine source based on cache status            if db_exists:                source = "taomarketcap"            else:                source = "taomarketcap-cache"            return subnets, source    except Exception as exc:        logger.warning("Error fetching from taomarketcap: %s", exc)        # Static fallback    logger.warning("Using static fallback data")    return [        {            "netuid": 29,            "name": "Coldint",            "emission": 3.0,            "apy": 42.5,            "volume": 1250000,            "market_cap": 45000000,            "price": 28.50,            "price_change_24h": 12.3,            "price_change_7d": 18.2,            "price_change_30d": 24.9,            "status": "active",            "sector": "AI/ML",        },        {            "netuid": 19,            "name": "Inference",            "emission": 2.1,            "apy": 38.2,            "volume": 980000,            "market_cap": 32000000,            "price": 15.20,            "price_change_24h": 8.7,            "price_change_7d": 12.1,            "price_change_30d": 16.8,            "status": "active",            "sector": "AI/ML",        },        {            "netuid": 12,            "name": "Compute",            "emission": 1.8,            "apy": 35.1,            "volume": 750000,            "market_cap": 28000000,            "price": 12.40,            "price_change_24h": 5.2,            "price_change_7d": 9.4,            "price_change_30d": 13.0,            "status": "active",            "sector": "Compute",        },    ], "static-fallback"def _safe_simivision_payload() -> Dict[str, Any]:    subnets, source = _get_subnets_with_source()    ranked = sorted(subnets, key=lambda s: (s.get("emission", 0), s.get("apy", 0), s.get("volume", 0)), reverse=True)    top = []    for idx, sn in enumerate(ranked[:3], start=1):        top.append({            "rank": idx,            "netuid": sn.get("netuid"),            "name": sn.get("name"),            "emission": sn.get("emission", 0),            "apy": sn.get("apy", 0),            "price_change_24h": sn.get("price_change_24h", 0),            "conviction": min(95, 72 + int(abs(sn.get("price_change_24h", 0))) + int(sn.get("apy", 0) / 4)),            "recommendation": "BUY" if idx == 1 else ("HOLD" if idx == 2 else "WATCH"),        })    return {        "status": "success",        "data": {            "top": top,            "meta": {                "count": len(subnets),                "source": source,                "updated_at": datetime.utcnow().isoformat() + "Z",            },        },    }def _safe_scenario_memory_summary() -> Dict[str, Any]:    """Return a lightweight scenario-memory summary without heavy computation."""    try:        data = scenario_memory._load()        scenarios = data.get("scenarios", [])        return {            "scenario_count": len(scenarios),            "last_scenario": scenarios[-1].get("name") if scenarios else None,            "last_updated": data.get("meta", {}).get("last_updated"),        }    except Exception as exc:        logger.warning("Could not load scenario memory summary: %s", exc)        return {"scenario_count": 0, "last_scenario": None, "last_updated": None}def _safe_rotation_summary() -> Dict[str, Any]:    """Return a lightweight rotation-tracker summary using cached subnet data."""    try:        subnets, _ = _get_subnets_with_source()        return rotation_tracker.get_rotation_summary(subnets)    except Exception as exc:        logger.warning("Could not load rotation tracker summary: %s", exc)        return {            "timestamp": datetime.utcnow().isoformat() + "Z",            "patterns": [],            "volatility_clusters": {},        }def _safe_pump_analytics() -> Dict[str, Any]:    """Return the Pump Cycle Analytics v2 payload for the homepage context.    Defensive: never raises — a tracker failure degrades to an empty payload so    the dashboard still renders.    """    try:        tracker = get_pump_tracker()        if tracker is None:            raise RuntimeError("pump tracker unavailable")        return tracker.get_all_analytics()    except Exception as exc:        logger.warning("Could not load pump analytics: %s", exc)        return {            "status": "success",            "data": {                "subnets": [],                "meta": {                    "tracked_subnets": 0,                    "total_cycles": 0,                    "avg_proneness": 0.0,                    "top_pump_candidates": [],                    "updated_at": datetime.utcnow().isoformat() + "Z",                },            },        }