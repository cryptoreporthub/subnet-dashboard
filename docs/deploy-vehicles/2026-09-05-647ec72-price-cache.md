# Deploy vehicle — 2026-09-05 · main @ 647ec72

Docs-only deploy vehicle. Ships the merged containment fix:

- #1194 — price-cache containment (load price_cache.json once per resolver cycle)

**Base deployed:** `8f4cb4c` (current production before this vehicle).
**This vehicle deploys:** `647ec72dbaea89626781eb5e17ccf065ee5217cf` (merged main with #1194).

Post-deploy verification: /health 200, /version 200 (new commit), /api/liveness prediction_resolver fresh, and read-only Patch D capture + liveness sample compare.
