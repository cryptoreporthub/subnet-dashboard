# Deploy vehicle — 2026-09-07 · main @ 0c15dda

Docs-only deploy vehicle. Ships the merged B1b checkpoint buckets + closing-invariant soak gate and the abandoned-worker flake fix already on main:

- #1205 — B1b: checkpoint buckets t0..t7 + closing-invariant soak gate (`gap_timing_ms` sibling of `stage_timing_ms`, `buckets_ms`)
- #1206 — test(resolver_scheduler): scope completed-write capture to the submitted cycle (flake fix)

**Base deployed:** `9ea0e3a` (production before this vehicle).
**This vehicle deploys:** `0c15dda36471e3447bf6ee0a5d037e0c4fe985fa` (merged main with #1205 + #1206).

Post-deploy verification: `/version` 200 == main short SHA `0c15dda`; fresh `sentry_release`; wait for one complete resolver cycle before reading `gap_timing_ms` (pre-cycle null is not a failure); confirm `gap_timing_ms` is a sibling of `stage_timing_ms` containing `buckets_ms` with `preflight` / `batch_bookkeeping` / `closing` keys.
