# R4 capture — post-fix resolver cycle logs (2026-09-05)

**Purpose:** Adjudicate between *cold-start artifact* and *steady-state failure* on build `8639a4a` (post-fix, deployed 11:33 AM AZ / 18:33Z).

## Context
- Deploy landed: 11:33 AM AZ (18:33Z) — build `8639a4a` (vehicle head; tree = main `647ec72` + docs, compare-verified)
- First post-deploy resolver tick: 11:44:41 AM AZ (18:44:41Z) → **cycle_timeout_180s**, resolved_now 0, pending 1, consecutive_failures 2
- Last success: 11:19 AM AZ (18:19:18Z) — **pre-deploy; zero successful post-fix cycles at trigger time**
- Resolver state at trigger: status failing ("last tick raised"), last_event stuck at 18:44:41Z
- Pre-capture liveness sample: **HUNG (>15s, ~19:00Z)** — mid-cycle hang, expected R4 signature

## Evidence bar (symmetric, same as G1 census)
1. Attribution of any ceiling hit to a concrete stage — hydration / sweep / peer calls / grading / contention — **cited to a specific raw.log line**, not inferred from timing alone.
2. **Success detection does NOT rely on /api/liveness** (it hangs during active cycles). Success = (a) external agent liveness samples at window start/end taken at responsive moments, showing last_success_at advanced past deploy time (18:33Z) and/or consecutive_failures reset; (b) steam raw.log absence of failure patterns is NOT itself proof of completion (per R2, completions are log-invisible).
3. Volume-stat before/after (workflow-native) = contention proxy; a STAT_FAIL is a data point, recorded.
4. If the logs don't answer the question, say so — same contract as the pre-fix census.

## Adjudication criteria
- **Cold-start supported:** a later cycle (2 or 3) completes — liveness last_success advances past 18:33Z; timeouts cluster only on the first post-restart cycle.
- **Steady-state failure confirmed:** subsequent cycles also hit the 180s ceiling (timeout events at ~15-min cadence); failure class reproduced on the post-fix build under settled conditions.
- **Indeterminate:** capture can't discriminate (e.g., resolver stops scheduling after the failed tick, no further events) — report honest indeterminate.

## Window
- Trigger: PR label `patchd-capture` (90-min default, label-triggered)
- Start: ~19:00Z (12:00 PM AZ) · End: ~20:30Z (1:30 PM AZ)
- Ticks expected in window: ~12:00, 12:15, 12:30, 12:45, 1:00, 1:15 (AZ) if cadence resumes
